pantheon_secrets.sync:
  path: '/admin/config/system/keys/pantheon'
  defaults:
    _form: '\Drupal\pantheon_secrets\Form\SyncForm'
    _title: 'Sync all Pantheon Secrets'
  requirements:
    _permission: 'sync pantheon_secrets keys'
